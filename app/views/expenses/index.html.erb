<h1>Expenses</h1>

<h2>This month: <%= number_to_currency(@sum_expenses_current_month, unit: current_user.currency) %></h2>

<%= render 'expenses_table', expenses: @expenses %>

<br>

<%= link_to 'New Expense', new_expense_path %>
